import { ctpAlumniNewsletter } from './ctp.alumni.newsletter';

describe('ctpAlumniNewsletter', () => {
  it('should work', () => {
    expect(ctpAlumniNewsletter()).toEqual('ctp.alumni.newsletter');
  });
});
